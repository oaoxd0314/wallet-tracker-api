host: localhost:8080
info:
  contact:
    name: nathan.lu
    url: https://tedmax100.github.io/
  description: Gin swagger
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  title: Gin swagger
  version: "1.0"
paths:
  /etherscan/gettraderecord:
    post:
      parameters:
      - description: wallet
        in: formData
        name: wallet
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: Get Wallet Found
      tags:
      - EtherScan
  /etherscan/getwalletfound:
    post:
      parameters:
      - description: wallet
        in: formData
        name: wallet
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: Get Wallet Found
      tags:
      - EtherScan
swagger: "2.0"
